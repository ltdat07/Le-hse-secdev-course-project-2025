services:
  studynotes:
    build:
      context: ..
      dockerfile: ../Dockerfile
    ports:
      - "8000:8000"
    environment:
      - PYTHONPATH=src
    read_only: true
    tmpfs:
      - /tmp
    security_opt:
      - no-new-privileges:true
    cap_drop:
      - ALL